TODO:

- Streaming::TokenIterator
    - takes an array of tokens and hands them out one at a time

- Streaming::TokenMatcher
    - given a Pointer, ... ???


Pipe->new(
    producer => Reader->new,
    consumer => Writer->new,
)->map()
